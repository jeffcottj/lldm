{
  "name": "@lldm/relay",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "dev": "wrangler dev --config ../../wrangler.jsonc",
    "test": "vitest --config ../../vitest.config.ts run apps/relay/src",
    "typecheck": "tsc --build"
  },
  "dependencies": {
    "@lldm/contracts": "workspace:*"
  }
}
